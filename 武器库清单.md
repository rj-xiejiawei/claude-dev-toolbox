# Claude Code 开发武器库完整清单

> **生成时间**: 2026-04-10
> **系统环境**: Ubuntu 24.04.4 LTS (WSL2)
> **Shell**: zsh + Oh My Zsh
> **项目路径**: `/mnt/d/code/other/feilun`

---

## 🖥️ 系统基础层

### 操作系统 & 环境
| 项目 | 版本 | 状态 |
|------|------|------|
| OS | Ubuntu 24.04.4 LTS (WSL2) | ✅ |
| Kernel | Linux 6.6.87.2-microsoft-standard-WSL2 | ✅ |
| Shell | zsh (默认) | ✅ |
| Oh My Zsh | 已安装 | ✅ |

### 版本管理工具
| 工具 | 版本 | 用途 | 安装路径 |
|------|------|------|----------|
| **nvm** | 0.39.5 | Node.js版本管理 | `~/.nvm` |
| **SDKMAN** | 5.22.4 | Java/多语言版本管理 | `~/.sdkman` |

### Shell增强工具
| 工具 | 版本 | 功能 | 状态 |
|------|------|------|------|
| **Oh My Zsh** | - | Zsh配置框架 | ✅ 主题: robbyrussell |
| **zsh-autosuggestions** | - | 命令自动建议 | ✅ |
| **fzf-tab** | - | 模糊补全Tab | ✅ |
| **fzf** | 0.44.1 | 模糊搜索器 | ✅ |
| **zsh-syntax-highlighting** | - | 语法高亮 | ⚠️ 可选 |

### 容器化工具
| 工具 | 版本 | 状态 |
|------|------|------|
| Docker | 29.4.0 | ✅ |
| docker-compose | 1.29.2 | ✅ |

### 基础工具链
```bash
✅ git
✅ curl
✅ wget
✅ vim/nano
```

---

## 🚀 Claude Code 生态

### 核心CLI工具
| 工具 | 版本 | 安装路径 | 用途 |
|------|------|----------|------|
| **Claude Code CLI** | 2.1.96 | `~/.nvm/versions/node/v24.14.1/bin/claude` | Anthropic官方CLI |
| **oh-my-claudecode** | 4.11.4 | `~/.claude/plugins/cache/omc/` | 多代理编排系统 |
| **skill-creator** | unknown | `~/.claude/plugins/cache/claude-plugins-official/` | 自定义技能创建 |
| **jdtls-lsp** | 1.0.0 | `~/.claude/plugins/cache/claude-plugins-official/` | Java LSP支持 |

### Node.js生态
| 工具 | 版本 | 管理方式 |
|------|------|----------|
| Node.js | v24.14.1 | nvm |
| npm | 内置 | nvm |
| npx | 内置 | nvm |

---

## 🔌 MCP 服务器

| MCP服务器 | 命令 | 用途 | 配置文件 |
|-----------|------|------|----------|
| **n8n-mcp** | `npx n8n-mcp` | 工作流自动化集成 | `.mcp.json` |
| **omc-bridge** | 内置 | oh-my-claudecode桥接 | 自动配置 |

---

## 🐦 飞书/Lark 集成生态

### Lark CLI
| 工具 | 版本 | 安装方式 |
|------|------|----------|
| @larksuite/cli | 1.0.7 | npm全局安装 |

### Lark Skills (20个)
```
✅ lark-approval          (审批流程)
✅ lark-base              (多维表格)
✅ lark-calendar          (日历管理)
✅ lark-contact           (通讯录)
✅ lark-doc               (文档)
✅ lark-drive             (云空间)
✅ lark-event             (事件订阅WebSocket)
✅ lark-im                (即时通讯)
✅ lark-mail              (邮箱)
✅ lark-minutes           (会议妙记)
✅ lark-openapi-explorer  (API探索)
✅ lark-shared            (共享基础)
✅ lark-sheets            (电子表格)
✅ lark-skill-maker       (技能创建器)
✅ lark-task              (任务管理)
✅ lark-vc                (视频会议)
✅ lark-whiteboard        (画板/图表)
✅ lark-wiki              (知识库)
✅ lark-workflow-meeting-summary  (会议纪要工作流)
✅ lark-workflow-standup-report   (日程待办摘要)
```

---

## 💻 编程语言环境

### Java (通过SDKMAN管理)
| 工具 | 状态 | 推荐版本 | 安装命令 |
|------|------|----------|----------|
| **JDK** | ⚠️ 未安装 | 17/21 | `sdk install java 17.0.12-tem` |
| **Maven** | ⚠️ 未安装 | 最新 | `sdk install maven` |
| **Gradle** | ❌ 未安装 | 可选 | `sdk install gradle` |

### Python
| 工具 | 版本 | 状态 |
|------|------|------|
| Python3 | 系统自带 | ✅ |
| pip3 | 系统自带 | ✅ |

### Go
| 工具 | 状态 | 推荐版本 | 安装命令 |
|------|------|----------|----------|
| **Go** | ❌ 未安装 | 最新 | `sudo apt install golang-go` |

### Rust (可选)
| 工具 | 状态 | 安装命令 |
|------|------|----------|
| **rustup** | ❌ 未安装 | `curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh` |

---

## 📦 项目依赖

### Backend (Spring Boot)
```xml
Spring Boot:    3.2.5
Java:           17 (需要安装)
构建工具:       Maven (需要安装)
数据库:         SQLite JDBC 3.45.1.0
JPA:            Spring Data JPA
Hibernate:      Community Dialects
```

### Frontend
```
Node.js:    24.14.1 (已安装)
包管理:     npm/npx
```

---

## 📁 配置文件结构

### Claude配置
```
~/.claude/
├── settings.json              # Claude CLI设置
├── .mcp.json                  # 插件MCP服务器
├── plugins/                   # 已安装插件
│   ├── oh-my-claudecode@4.11.4
│   ├── skill-creator
│   └── jdtls-lsp@1.0.0
├── skills/                    # 自定义skills (链接到项目)
└── projects/                  # 项目记忆
```

### 项目MCP配置
```
.mcp.json                      # 项目级MCP (n8n配置)
```

### Zsh配置
```
~/.zshrc                       # 主配置文件
~/.oh-my-zsh/                  # Oh My Zsh安装
~/.oh-my-zsh/custom/plugins/   # 自定义插件
├── zsh-autosuggestions/
├── fzf-tab/
└── example/
```

### 版本管理器配置
```
~/.nvm/                        # nvm (Node.js)
~/.sdkman/                     # SDKMAN (Java/多语言)
```

### Lark配置
```
~/.lark-cli/config.json        # 飞书CLI配置
```

---

## 🔑 认证配置清单

### Claude API (必需)
```bash
# ~/.claude/settings.json 或环境变量
export ANTHROPIC_AUTH_TOKEN="sk-ant-xxxxx"
export ANTHROPIC_BASE_URL="https://open.bigmodel.cn/api/anthropic"
```

### Lark/飞书 (可选)
```bash
lark-cli auth login
# 配置文件: ~/.lark-cli/config.json
```

### n8n MCP (可选)
```bash
# ~/.mcp.json
export N8N_API_URL="http://localhost:5678"
export N8N_API_KEY="your-api-key"
```

### Docker (如需私有镜像)
```bash
# ~/.docker/config.json
docker login
```

---

## 🛠️ 一键安装命令

### 完整安装脚本
```bash
# 方式1: 使用skill
/dev-toolbox-install

# 方式2: 直接运行
./.agents/skills/dev-toolbox-install/install.sh

# 安装选项
--no-java      # 跳过Java/Maven安装
--with-go      # 安装Go
--no-lark      # 跳过Lark skills
--no-n8n       # 跳过n8n配置
```

### 分步安装命令

#### 1. 系统基础工具
```bash
# Oh My Zsh (如果未安装)
sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

# zsh插件
git clone https://github.com/zsh-users/zsh-autosuggestions ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/zsh-autosuggestions
git clone https://github.com/Aloxaf/fzf-tab ${ZSH_CUSTOM:-~/.oh-my-zsh/custom}/plugins/fzf-tab

# fzf (如果未安装)
sudo apt install fzf
```

#### 2. 版本管理器
```bash
# nvm (Node.js)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.5/install.sh | bash

# SDKMAN (Java/Maven/Gradle)
curl -s "https://get.sdkman.io" | bash
```

#### 3. Claude Code生态
```bash
# Claude Code CLI
npm install -g @anthropic-ai/claude-code

# oh-my-claudecode插件
claude plugin install --source omc oh-my-claudecode

# 认证
claude auth login
```

#### 4. Lark集成
```bash
# Lark CLI
npm install -g @larksuite/cli

# 安装所有skills
lark-cli skill install --all

# 认证
lark-cli auth login
```

#### 5. Java开发环境
```bash
# 通过SDKMAN安装Java 17
sdk install java 17.0.12-tem

# 安装Maven
sdk install maven

# 设置默认版本
sdk default java 17.0.12-tem
```

#### 6. Go (可选)
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install golang-go

# 或官方安装
wget https://go.dev/dl/go1.22.0.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.22.0.linux-amd64.tar.gz
```

#### 7. MCP服务器
```bash
# n8n-mcp
npm install -g n8n-mcp

# 配置文件 (~/.mcp.json 或项目.mcp.json)
cat > ~/.mcp.json << 'EOF'
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error"
      }
    }
  }
}
EOF
```

---

## 📊 环境统计总览

### 安装统计
- **总Skills数**: 41个 (20 Lark + 21 自定义)
- **Claude插件**: 4个
- **MCP服务器**: 2个活跃
- **Zsh插件**: 3个核心插件
- **版本管理器**: 2个 (nvm, SDKMAN)

### 版本总览
```
Node.js:     v24.14.1 (LTS)
nvm:         v0.39.5
SDKMAN:      v5.22.4
Docker:      v29.4.0
fzf:         v0.44.1
Claude CLI:  v2.1.96
OMC插件:     v4.11.4
Lark CLI:    v1.0.7
```

---

## 🎯 下一步建议

### 必需项
1. ✅ 安装Java 17: `sdk install java 17.0.12-tem`
2. ✅ 安装Maven: `sdk install maven`
3. ✅ 配置Claude API认证

### 推荐项
4. ⚠️ 安装zsh-syntax-highlighting (语法高亮)
5. ⚠️ 配置n8n工作流集成

### 可选项
6. 💡 安装Go (如需Go工具)
7. 💡 安装Rust (如需Rust工具链)

---

## 🚨 常见问题

### SDKMAN离线问题
```bash
# 禁用启动检查
mkdir -p ~/.sdkman/etc
echo "sdkman_healthcheck_enable=false" > ~/.sdkman/etc/config
```

### Java版本切换
```bash
# 查看已安装版本
sdk list java

# 切换临时版本
sdk use java 17.0.12-tem

# 设置默认版本
sdk default java 17.0.12-tem
```

### Node版本切换
```bash
# 查看可用版本
nvm list-remote

# 安装特定版本
nvm install 20

# 切换版本
nvm use 20
```

---

## 📝 配置备份

### 备份命令
```bash
# 备份zsh配置
cp ~/.zshrc ~/.zshrc.backup

# 备份Claude配置
tar -czf claude-backup.tar.gz ~/.claude/

# 备份NPM全局包
npm list -g --json > npm-global-backup.json
```

### 恢复命令
```bash
# 恢复zsh配置
cp ~/.zshrc.backup ~/.zshrc

# 恢复Claude配置
tar -xzf claude-backup.tar.gz -C ~/

# 恢复NPM全局包
cat npm-global-backup.json | jq -r '.dependencies | keys[]' | xargs npm install -g
```

---

**生成工具**: Claude Code + oh-my-claudecode
**最后更新**: 2026-04-10
**维护者**: crab
**环境**: WSL2 Ubuntu 24.04.4 LTS
